<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Redirecting...</title>
    <script type="text/javascript">
      window.localStorage.setItem('accessToken', {{{literal accessToken}}});
      {{#if currentCharacterId}}
        window.localStorage.setItem('currentCharacterId', {{{literal currentCharacterId}}});
      {{/if}}
      window.location.replace({{{literal redirectPath}}});
    </script>
  </head>
<body>

</body>
</html>
